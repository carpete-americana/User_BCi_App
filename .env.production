# Configurações de Produção
# Este ficheiro é incluído no instalador final da app

# Debug desactivado em produção
DEBUG=true

# NOTA: Chave de encriptação é gerada automaticamente na primeira execução
# Cada instalação terá a sua própria chave única
# ENCRYPTION_KEY= (vazio - gerado automaticamente)

# NOTA: GITHUB_TOKEN não é incluído em produção
# A app carrega assets do GitHub sem autenticação (rate limit: 60 requests/hora)
# Suficiente para uso normal da aplicação
